# 运行时 Story 数据边界

本目录预留给未来独立于任务定义的运行时 Story 数据。当前 StoryRunner 以 `content/quests/**.json` 的任务节点为剧情来源，因此本目录没有可加载 JSON，也没有加入 `content/manifest.json`。

受审剧本、原始交付包、审阅稿和 fixtures 不属于运行时内容，分别留在 `docs/story/` 或 `content/tests/fixtures/`，不得复制到这里规避 Manifest 门禁。
