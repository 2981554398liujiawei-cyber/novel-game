# 测试夹具目录

仅存放自动测试使用的测试内容与状态快照。这里的文件不得被 `content/manifest.json` 注册，也不得由正式运行时加载。

`quest_manager/`中的任务和状态只验证任务生命周期、目标、依赖、失败续接及存档往返，不属于正式剧情，也不得提升为正式内容源。

`combat_runner/`中的战斗、敌人、技能、状态和AI均为`TEST_`合成数据，只用于固定种子的战斗运行与错误边界验证；正式构建仅初始化CombatRunner代码和旧内容索引，不打包这些夹具。
