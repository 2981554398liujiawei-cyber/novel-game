# 剧情原件接收区

这里保存制作方提供的原始附件，永远不由运行时加载。

- `incoming/`：刚收到、尚未人工批准的压缩包；只能做只读预检。
- `active/`：与 `source_manifest.json` 对应的当前篇章权威原件。
- `roadmap/`：未来路线原件，不得影响当前第七新手村。
- `reference/`：历史或未来参考原件，不得直接转正式数据。

压缩包通过预检不等于剧情被批准。只有人工审核后，完整剧本 Markdown 才能进入 `docs/story/scripts/`，随后按内容状态门转换。
