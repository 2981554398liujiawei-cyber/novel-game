# 存档开发期兼容策略

## Prototype 阶段

技术骨架到垂直切片首次签收前，开发存档允许失效。每次破坏性修改必须：

- 提高开发存档标记或清理旧测试存档；
- 在变更说明中写明“不兼容旧开发存档”；
- 不投入时间维护大量一次性迁移脚本。

## 兼容冻结点

垂直切片首次正式签收后，进入存档兼容冻结：

- 所有已签收版本必须有明确 `save_version`；
- 结构变化必须提供迁移或显式拒绝；
- 正式构建不得静默丢弃旧存档；
- 必须保留备份恢复路径。
